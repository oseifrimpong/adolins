<%= form_for([@post, @post.comments.build]) do |f| %>
	<%= f.text_area :content, :placeholder => "Add your comment here", required: true, class: 'form-control', row: '3' %>
	<%= f.submit 'Comment', class: 'btn-primary' %>
<% end %>